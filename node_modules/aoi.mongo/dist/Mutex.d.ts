/** Type of a mutex queue entry */
export declare type QueueEntry = () => void;
/** Mutex for locker utility */
export declare class Mutex {
    _locked: boolean;
    _queue: QueueEntry[];
    /** Lock the mutex */
    lock(): Promise<void>;
    /** Unlock the mutex */
    unlock(): void;
}
